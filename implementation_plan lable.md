# پلن جامع پیاده‌سازی سیستم چاپ لیبل حرارتی خالص تحت وب (Pure Web App)

این پروژه به صورت **کاملاً تحت وب (PWA / Web App)** پیکربندی شده و تمام کدهای نیتیو اندروید و Capacitor از آن حذف گردیده‌اند. موتور چاپ بر پایه **استانداردهای وب (`window.print()`)** و **خروجی مستقیم Vector PDF** برای پرینتر حرارتی **Woosim WSP-R350** و تمام مرورگرها کار خواهد کرد.

---

## 🚀 اسکیل‌های نصب‌شده و فعال در این پروژه
1. **`superpowers`**: انضباط مهندسی، عیب‌یابی ریشه‌ای (Systematic Debugging) و تست صحت قبل از اعلام پایان.
2. **`ui-ux-pro-max`**: استاندارد عالی تایپوگرافی، کنتراست بالا، پالت‌های رنگی و طراحی پریمیوم UI/UX.
3. **`design-dna`**: استخراج کدهای رنگی و الگوهای طراحی برای شبیه‌سازی دقیق اتیکت‌های استاندارد.

---

## 🛑 تغییرات انجام شده در مخزن
- [x] **حذف پوشه نیتیو `android/`**.
- [x] **حذف پیکربندی `capacitor.config.ts`**.
- [x] **پاک‌سازی پکیج‌های `@capacitor/*`** از `package.json`.

---

## 🚨 ۱. باگ‌های بحرانی که باید برطرف شوند

### باگ ۱: خطای رنگی `oklch color function`
- **ریشه مشکل:** تداخل Tailwind CSS v4 با `html2canvas` هنگام تبدیل HTML به تصویر.
- **راهکار اجرا:** جایگزینی `html2canvas` با **`html-to-image`** یا خروجی مستقیم وکتور در `jsPDF`.

### باگ ۲: باگ محاسباتی قیمت در `src/api.ts`
- **ریشه مشکل:** تقسیم اجباری قیمت بر ۱۰ (`num / 10`).
- **راهکار اجرا:** حذف `num / 10` و حفظ قیمت اصلی دریافت شده از گوگل شیت.

### باگ ۳: نشتی حافظه صوتی (`AudioContext Leak`)
- **راهکار اجرا:** فراخوانی `ctx.close()` پس از پخش بوق اسکنر در `CameraScanner.tsx`.

### باگ ۴: تاریخ ثابت روز به جای تاریخ کالا
- **راهکار اجرا:** استفاده از `product.productionDate` یا `product.expirationDate` در صورت وجود.

---

## 🖨️ ۲. تنظیمات اختصاصی پرینتر Woosim WSP-R350 و چاپ تحت وب

### مشخصات چاپ
- **عرض مفید چاپ:** ۷۲ میلی‌متر (۵۷۶ پیکسل)
- **رزولوشن:** 203 DPI
- **عملکرد چاپ:** دیالوگ چاپ استاندارد مرورگر (`window.print()`) با استایل‌های دقیق `@media print`.

### تغییرات ضروری در CSS چاپ وب (`src/index.css`)
1. **حذف کامل `transform: rotate(90deg)`** و چرخش‌های نرم‌افزاری.
2. **تنظیم ابعاد برگه چاپ:** `@page { size: 72mm 40mm; margin: 0; }`.
3. **صفر کردن تمام پدینگ‌ها و حاشیه‌ها** برای لبه‌به‌لبه شدن چاپ حرارتی.
4. **تنظیم ضخامت بارکد:** مقدار `width={2}` و فرمت **`CODE128`** در `react-barcode`.
5. **استفاده از React Portal:** برای رندر کامل فونت Vazirmatn و آیکون‌ها قبل از فراخوانی `window.print()`.

---

## 🧹 ۳. پاک‌سازی کدها و معماری ساختاری

1. **حذف اسکریپت‌های `patch_*.cjs`** و تثبیت تغییرات مستقیماً در سورس TypeScript.
2. **ایجاد فایل توابع عمومی (`src/utils/formatters.ts`)** برای اعداد فارسی و فرمت قیمت.
3. **ادغام کامپوننت‌های تکراری اسکنر دوربین**.

---

## 📝 پرامپت نهایی جهت اعطا به Claude Opus 4.6

```markdown
پروژه ۱۰۰٪ تحت وب است و سه اسکیل superpowers، ui-ux-pro-max و design-dna در آن نصب شده است. لطفاً کدهای پروژه را با رعایت استانداردهای این اسکیل‌ها بازنویسی کنید:

۱. باگ oklch: در فایل src/utils/pdfPrint.ts پکیج html2canvas را با html-to-image جایگزین کنید تا ارور oklch حل شود.
۲. باگ قیمت: در src/api.ts تقسیم بر ۱۰ را حذف کنید.
۳. پرینتر Woosim WSP-R350: در src/index.css چرخش rotate(90deg) را کلاً حذف کرده و @page را روی size: 72mm 40mm; margin: 0; بگذارید.
۴. بارکد: فرمت را به CODE128 و width={2} تغییر دهید.
۵. زیبایی UI/UX: طبق اسکیل ui-ux-pro-max و design-dna، اتیکت‌ها را با تایپوگرافی Vazirmatn، کنتراست فوق‌العاده بالا و بج‌های وکتوری ۷۲ میلی‌متری طراحی کنید.
۶. پاک‌سازی: اسکریپت‌های patch_*.cjs را حذف کرده و توابع عمومی را به src/utils/formatters.ts منتقل کنید.
```
