# Superpowers Framework

Before doing anything else, check available skills in `.agents/skills` or `skills/` (e.g. `using-superpowers`, `brainstorming`, `systematic-debugging`, `test-driven-development`, `writing-plans`, `executing-plans`, `subagent-driven-development`, `verification-before-completion`). Always invoke relevant skills before taking action.
